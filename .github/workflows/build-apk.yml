name: Build Android APK

on:
  push:
  pull_request:
  workflow_dispatch: # Allows you to manually trigger the build from GitHub Actions UI

jobs:
  build:
    name: Build Web App and Compile APK
    runs-on: ubuntu-latest

    steps:
      # 1. Check out the repository
      - name: Checkout Code
        uses: actions/checkout@v4

      # 2. Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      # 3. Install npm dependencies
      - name: Install Dependencies
        run: npm ci

      # 4. Build the Vite React Frontend
      - name: Build Web Application
        run: npm run build

      # 5. Set up Java JDK (Required for Android builds)
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      # 6. Install Android SDK Tools
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      # 7. Sync compiled web assets into the Capacitor Android project
      - name: Sync Capacitor Assets
        run: npx cap sync android

      # 8. Build the Android APK using Gradle
      - name: Build APK with Gradle
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug # Produces a debug APK

      # 9. Upload the resulting APK as a build artifact
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
